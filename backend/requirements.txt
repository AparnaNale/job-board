fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-dotenv==1.0.1
python-multipart==0.0.9
pdfplumber==0.11.1
python-docx==1.1.2
httpx==0.27.0
pydantic==2.7.4
passlib[bcrypt]==1.7.4
# Pinned explicitly: passlib 1.7.4's bcrypt version-detection code
# (detect_wrap_bug) crashes with "password cannot be longer than 72 bytes"
# on bcrypt>=4.1, even for short passwords -- it's a known passlib/bcrypt
# incompatibility, not an actual password-length problem. bcrypt 4.0.1 is
# the last version passlib 1.7.4 detects correctly.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0